# image-generation-with-pre-trained-model
Generate high-quality images using a pre-trained deep learning model. Supports text prompts or latent inputs and runs on CPU/GPU. Quick setup for experimentation with generative AI.
